rootProject.name = 'source-dep-classpath-issue'

sourceControl {
    gitRepository("injected-repo/") {
        producesModule("injected.plugin.group:an-artifact")
        plugins {
            id "utilities-build"
        }
    }
}

